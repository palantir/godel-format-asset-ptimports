type: improvement
improvement:
  description: |-
    Determine project module import path and use that as input to
    local import sorting rather than loading package path.
  links:
  - https://github.com/palantir/godel-format-asset-ptimports/pull/99
